---
title: "数学公式与 MDX 示例"
description: "验证 LaTeX、代码高亮和 MDX 组件嵌入能力。"
date: 2026-06-19
tags: ["Astro", "LaTeX", "MDX"]
draft: false
---

import Note from "../../components/Note.astro";

这篇文章用于验证技术写作能力：数学公式、代码块和 MDX 组件。

## LaTeX 公式

行内公式可以写成 $E = mc^2$。

块级公式可以写成：

$$
\mathcal{L} = - \sum_{i=1}^{n} y_i \log \hat{y}_i
$$

## 代码高亮

```ts
type Post = {
  title: string;
  tags: string[];
  draft: boolean;
};

const published = posts.filter((post: Post) => !post.draft);
```

## MDX 组件

<Note title="MDX 的使用边界">
  普通文章继续用 Markdown。只有需要嵌入组件、交互内容或特殊展示时，才使用 MDX。
</Note>

